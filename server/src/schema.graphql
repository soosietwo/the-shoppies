type Nominee {
  id: ID!
  title: String!
  poster: String
  year: Int!
}

type Query {
  nominees: [Nominee]!
}

type Mutation {
  addNominee(id: ID!, title: String!, poster: String, year: Int!): Nominee!
  deleteNominee(id: ID!): Nominee!
}
